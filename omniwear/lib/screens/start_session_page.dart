import 'package:flutter/material.dart';

class StartSessionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Start Session'),
      ),
      body: const Center(
        child: Text('Start Session Page'),
      ),
    );
  }
}
